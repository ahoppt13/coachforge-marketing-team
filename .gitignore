# Secrets — never commit
.env
*.key
config/credentials*

# OS
.DS_Store

# Automation run logs (keep the dir, not the logs)
automation/logs/*
!automation/logs/.gitkeep

# Rendered carousel PNGs (generated artifacts — keep the renderer, not the output)
carousel/out/
